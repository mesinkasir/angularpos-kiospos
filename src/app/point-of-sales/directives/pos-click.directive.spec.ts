import { PosClickDirective } from './pos-click.directive';

describe('PosClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PosClickDirective();
    expect(directive).toBeTruthy();
  });
});
